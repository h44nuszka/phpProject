{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_tag_list'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'title_tag_list'|trans }}</h1>
    {% if pagination is defined and pagination.items|length %}
        <div class="container">
            <ul class="row nav nav-pills">
                <li><a href="{{ url('tag_create') }}" class="btn btn-primary btn-alter" title="{{ 'label_add'|trans }}">
                        <i class="bi bi-calendar-plus"></i>
                    </a></li>
            </ul>


            <div class="col"></div>
            <div class="col">
                <table class="table table-striped category_table">
                    <thead>
                    <tr>
                        <th>{{ knp_pagination_sortable(pagination, 'label_title'|trans, 'tag.title') }}</th>
                        <th>{{ knp_pagination_sortable(pagination, 'label_created_at'|trans, 'tag.createdAt') }}</th>
                        <th>{{ knp_pagination_sortable(pagination, 'label_updated_at'|trans, 'tag.updatedAt') }}</th>
                        <th>{{ 'label_actions'|trans }}</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for tag in pagination.items %}
                        <tr>
                            <td>{{ tag.title }}</td>
                            <td>{{ tag.createdAt|format_date('long') }}</td>
                            <td>{{ tag.updatedAt|format_date('long') }}</td>

                            <td>

                                <a href="{{ url('tag_edit', {id: tag.id}) }}" title="{{ 'action_edit'|trans }}" class="iconlink">
                                    <i class="bi bi-pencil"></i>
                                </a>
                                {% if not tag.events|length %}
                                    <a href="{{ url('tag_delete', {id: tag.id}) }}" title="{{ 'action_delete'|trans }}" class="iconlink">
                                        <i class="bi bi-trash"></i>
                                    </a>
                                {% endif %}
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
            <div class="col"></div>
        </div>
        <div class="navigation text-center d-flex justify-content-center paginator-center">
            {{ knp_pagination_render(pagination) }}
        </div>

    {% else %}
        {% include 'default/_single_flash_message.html.twig' with {
            type: 'warning',
            message: 'message_empty_list'|trans

        } %}
    {% endif %}
{% endblock %}